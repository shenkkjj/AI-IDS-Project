# Run: M1-DEMO-FLOW

开始时间：2026-06-15
运行模式：L2，无人值守长任务
预算：最长 3 小时，同一失败最多修复 3 轮；diff 超过约 800 行且非文档/生成文件时停止总结

## 目标

- 建立一个端到端 Demo Flow：模拟安全事件 -> 后端产生/读取告警 -> 前端 Dashboard 可见 -> Copilot 能对告警进行分析或展示清晰降级态。
- 优先复用现有 FastAPI / Next.js 架构，不做大重构，不降低测试质量，不删除业务测试。

## 范围

允许修改：
- `scripts/**`
- `docs/**`
- `server/tests/**`
- `web-next/**` 中与 demo 展示直接相关的代码
- 小范围修改 `server/routers/**`、`server/services/**` 以打通已有告警/Copilot 流程

禁止修改：
- 真实 `.env`
- `.claude/settings.local.json`
- git 历史
- 认证、授权、JWT、cookie、安全护栏核心策略
- 数据库 schema / migration
- 部署密钥、生产配置
- 删除未知文件或弱化业务测试

## 启动前状态

已读取：
- `AGENTS.md`
- `PRODUCT.md`
- `docs/agent/UNATTENDED_LONG_TASKS.md`
- `README.md`

已使用 skill：
- `terminal-ops`
- `python-testing`
- `frontend-patterns`
- `product-capability`
- `dashboard-builder`
- `verification-loop`

启动前 `git status --short` 已有改动：
- `M .claude/settings.local.json`（禁止范围，不触碰）
- `M .coverage`（覆盖率产物，不触碰）
- `M .github/workflows/ci.yml`（前序任务改动）
- `M AGENTS.md`（既有改动，不触碰）
- `M README.md`（前序任务改动，本任务可能追加 demo 说明）
- `M docs/ALEMBIC_MIGRATION.md`（既有改动，不触碰）
- `M docs/RELEASE_NOTES.md`（既有改动，不触碰）
- `M server/STRUCTURE.md`（既有改动，不触碰）
- `M server/tests/conftest.py`（前序任务改动）
- `M server/tests/test_e2e.py`（前序任务改动）
- `M web-next/package.json`（前序任务改动，本任务如需新增脚本会谨慎追加）
- `?? PRODUCT.md`、`?? docs/agent/`、`?? docs/runs/`（前序文档/日志未跟踪，本任务在 `docs/runs/2026-06-15-m1-demo-flow.md` 继续记录）
- `?? server/.coverage`（覆盖率产物，不触碰）

## 计划

- [x] 阶段 1：读取后端告警/Copilot 路由、服务、模型与现有测试，确定最小 demo 接入点。
- [x] 阶段 2：读取前端 dashboard/hooks/types，确认 Dashboard 如何展示告警和 Copilot 状态。
- [x] 阶段 3：实现 demo seed/script/API 或前端 demo 入口，并补 smoke test。
- [x] 阶段 4：同步 README / PRODUCT / 本运行日志，做 de-sloppify。
- [x] 阶段 5：运行后端测试、Guardrails 专项测试、前端 typecheck/build。

## 阶段记录

### 阶段 1：基线探索

改动：
- 创建本运行日志。
- 读取 `server/routers/alerts_router.py`、`server/services/alert_service.py`、`server/routers/copilot_router.py`、`server/services/copilot_service.py`、`server/models/schemas.py`、`server/core/state.py`。
- 读取 `web-next/hooks/useAlerts.ts`、`web-next/hooks/useCopilot.ts`、`web-next/app/dashboard/dashboard-client.tsx`、`web-next/components/dashboard/*`。

验证：
- `git status --short; git diff --stat`
- `.\.venv\Scripts\python.exe -m pytest server\tests\test_demo_flow.py -q --tb=short`（RED：新增测试前接口为 404）

结果：
- 后端已有 `/alerts` ingest、`/alerts` 读取、内存 backlog、WebSocket 广播和 Copilot `alert_id` 上下文拼接。
- `/alerts` ingest 需要内部 `ALERTS_INGEST_TOKEN`，不适合直接暴露给浏览器；决定新增认证态 `/alerts/demo`，只允许当前用户触发固定 demo 场景。
- Copilot 缺少 API key 时已有 SSE 降级错误，但前端还缺“分析当前告警”的演示快捷动作。

下一步：
- 实现固定 demo attack API、前端按钮和脚本。

### 阶段 2：后端 demo attack API

改动：
- 新增 `server/tests/test_demo_flow.py`。
- `server/services/alert_service.py` 新增固定 `DEMO_ATTACK_SCENARIOS` 和 `trigger_demo_attack`，同步写入 backlog 并广播给当前用户。
- `server/routers/alerts_router.py` 新增认证态 `POST /alerts/demo`。

验证：
- `$env:APP_SECRET='test-local-secret-key-for-baseline-32chars'; $env:AUTH_SECRET='test-local-auth-secret-for-baseline-32chars'; .\.venv\Scripts\python.exe -m pytest server\tests\test_demo_flow.py -q --tb=short`

结果：
- 2 passed。
- 安全边界：接口必须登录；场景名使用 `Literal` 白名单；告警归属强制绑定当前用户；不暴露内部 ingest token；不改数据库 schema。

下一步：
- 前端接入一键触发与 Copilot 快捷分析。

### 阶段 3：前端 Demo Flow 与脚本

改动：
- `web-next/hooks/useAlerts.ts` 新增 `triggerDemoAttack` 和 `demoState`，调用 `/api/backend/alerts/demo` 后把返回告警并入现有 Dashboard 数据流。
- `web-next/app/dashboard/dashboard-client.tsx` 在实时告警区新增“触发 Demo 攻击”，触发后选中新告警并写入终端日志。
- `web-next/components/dashboard/CopilotPanel.tsx` 新增上下文提示和“分析当前告警”快捷动作。
- `web-next/types/terminal.ts`、`web-next/utils/terminalUtils.ts` 增加 success tone。
- `scripts/demo_attack.ps1` 新增半自动 demo smoke 脚本：health -> register/login -> `/alerts/demo` -> `/alerts` -> `/copilot/stream`。
- `web-next/app/api/backend/[...path]/route.ts` 清理调试输出。

验证：
- `$env:APP_SECRET='test-local-secret-key-for-baseline-32chars'; $env:AUTH_SECRET='test-local-auth-secret-for-baseline-32chars'; .\.venv\Scripts\python.exe -m pytest server\tests\test_demo_flow.py -q --tb=short`
- `cd web-next; npm run typecheck`

结果：
- Demo smoke test：4 passed, 17 warnings（NeMo/Pydantic 第三方弃用警告）。
- 前端 typecheck 通过。

下一步：
- 同步 README / PRODUCT，并运行最终验证。

### 阶段 4：文档同步与 de-sloppify

改动：
- `README.md` 新增 Demo Flow：模拟攻击闭环说明和 `scripts/demo_attack.ps1` 用法。
- `PRODUCT.md` 更新 M1 状态：基础闭环已建立，同时保留 audit 展示、真实浏览器 E2E、Guardrails 拦截态 UI 等产品债务。
- 清理 `web-next/app/api/backend/[...path]/route.ts` 中代理调试输出。
- `scripts/demo_attack.ps1` 改为 ASCII 输出，避免 Windows PowerShell 控制台中文编码影响演示。

验证：
- `rg -n "console\.|/alerts/demo|triggerDemoAttack|Demo Flow|demo_attack|DEMO_ATTACK|分析当前告警" ...`
- `git diff --check -- <本任务相关文件>`

结果：
- 本轮新增代理调试输出已清理；剩余 `console.error` 位于既有 error boundary / auth 登录失败日志，不属于本任务新增。
- `git diff --check` 无 whitespace 错误。

下一步：
- 运行最终验证。

### 阶段 5：最终验证

改动：
- 无新增代码改动，仅执行验证与记录结果。

验证：
- `$env:APP_SECRET='test-local-secret-key-for-baseline-32chars'; $env:AUTH_SECRET='test-local-auth-secret-for-baseline-32chars'; .\.venv\Scripts\python.exe -m pytest server\tests -q --tb=short`
- `$env:APP_SECRET='test-local-secret-key-for-baseline-32chars'; $env:AUTH_SECRET='test-local-auth-secret-for-baseline-32chars'; .\.venv\Scripts\python.exe -m pytest server\tests\security\llm_guardrails -q --tb=short`
- `cd web-next; npm run typecheck`
- `cd web-next; npm run build`
- 启动本地后端/前端 dev server 后运行：`.\scripts\demo_attack.ps1 -BaseUrl http://127.0.0.1:8000 -Email demo-analyst@example.com -Password DemoPass123! -Scenario sql_injection`

结果：
- 后端默认测试：223 passed, 1 skipped, 17 warnings。
- Guardrails 专项测试：139 passed, 17 warnings。
- 前端 build：通过。
- 前端 typecheck：先与 build 并行执行时因 `.next/types` 被并发清理出现 TS6053；顺序重跑 `npm run typecheck` 后通过。结论：不要并行执行 `next typegen/tsc` 与 `next build`。
- Demo 脚本：通过。验证 health、注册/登录、`/alerts/demo`、`/alerts` 可见性、`/copilot/stream` SSE 或降级态。
- 本轮启动的本地后端/前端进程已停止。

浏览器验证：
- 本轮尝试搜索可用 Browser 工具，但当前工具列表未暴露可用的本地浏览器控制接口；因此以真实本地 HTTP demo smoke 作为替代验证，未生成截图级证据。

## 验证证据

- `server/tests/test_demo_flow.py`：4 passed, 17 warnings。
- `server/tests`：223 passed, 1 skipped, 17 warnings。
- `server/tests/security/llm_guardrails`：139 passed, 17 warnings。
- `web-next npm run typecheck`：通过。
- `web-next npm run build`：通过。
- `scripts/demo_attack.ps1`：通过，输出 `Demo flow complete.`

## 未解决问题

- 全仓已有多处未提交改动，本任务只在允许范围内叠加，不回滚既有改动。
- 当前 demo 告警存在内存 backlog 中，服务重启后会消失；这符合 M1 demo 目标，但不是持久化 SOC 审计。
- `/alerts/demo` 已绑定认证用户并限制固定场景，但还没有更细粒度的频率限制；后续可纳入 M1/M2。
- Guardrails audit 已在 Copilot 路径记录，但 Dashboard 尚未展示 demo attack / Copilot / Guardrails 的统一审计时间线。
- 本轮未完成真实 Playwright 浏览器 E2E 截图验证。

## 最终状态

完成。
